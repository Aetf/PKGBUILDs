diff --git a/src/core/qtlibtorrent/qbtsession.cpp b/src/core/qtlibtorrent/qbtsession.cpp
index cd46234..1034d6b 100755
--- a/src/core/qtlibtorrent/qbtsession.cpp
+++ b/src/core/qtlibtorrent/qbtsession.cpp
@@ -1618,7 +1624,7 @@ qreal QBtSession::getRealRatio(const libtorrent::torrent_status &status) const {
   }
 
   qreal ratio = all_time_upload / (float) all_time_download;
-  Q_ASSERT(ratio >= 0.);
+  //Q_ASSERT(ratio >= 0.);
   if (ratio > MAX_RATIO)
     ratio = MAX_RATIO;
   return ratio;
